{
  "name": "antrosys-erp",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "dotenv -e .env -- pnpm --filter api exec prisma migrate dev --name schema_update",
    "db:seed": "dotenv -e .env -- pnpm --filter api exec prisma db seed"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "turbo": "^2.3.0"
  }
}
